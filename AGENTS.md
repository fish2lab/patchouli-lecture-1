# AGENTS.md

## 这个仓库做什么

做一个知识科普动画：《我是帕秋莉，我来教你调理身体！》，「帕秋莉讲座」系列第 1 集。需求原文、知识来源、参考实现、角色参考图都在 `README.md` 和 `refs/`。内容调查、分镜、技术选型由你决定，取舍写进 `docs/`。

## 测试和 CI：不要过度

- 不搭大规模 E2E 套件，不为凑覆盖率写测试。断言常量字符串含某段子串这类为通过而写的测试一律不写。
- 开发中只跑和本次改动相关的一两条检查（例如构建能过、页面能打开、关键动画能播完一遍截个图）。
- CI 最多一条关键路径：构建 + 部署（如 GitHub Pages）。不加矩阵、不加多浏览器、不加视觉回归。
- 验收靠实际打开页面看效果，不靠测试数量。

## 内容

- 健康知识以知识来源仓库为主，额外调查的结论标出处；不写医疗诊断和药物剂量建议。
- 面向大学生：例子贴近宿舍、熬夜、久坐、外卖、考试周。
- 角色形象参考 `refs/character/`，不直接把参考图当成品素材发布（版权不明），需要时重绘或自绘矢量。
